/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["https://lh3.googleusercontent.com"], // Adicione o domínio do Google para imagens externas
  },
};

export default nextConfig;